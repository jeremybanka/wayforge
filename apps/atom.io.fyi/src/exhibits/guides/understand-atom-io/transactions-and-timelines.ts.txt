import { timeline, transaction } from "atom.io"

const saveDraftTX = transaction<() => void>({
	key: `saveDraft`,
	do: ({ set }) => {
		set(statusAtom, `saving`)
		set(savedAtAtom, Date.now())
	},
})

const editsTL = timeline({
	key: `edits`,
})
