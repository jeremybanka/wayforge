---
"atom.io": minor
---

💥 BREAKING CHANGE: `atom.io/immortal` and `atom.io/ephemeral` have been removed. Instead of importing `seekState` or `findState` from them respectively, instead prefer to import `findState` from `atom.io` when strictly necessary. Generally prefer streamlined get/set.
