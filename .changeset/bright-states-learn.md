---
"atom.io": patch
---

📝 Expanded the getting-started docs for evaluating `atom.io` in existing React apps.

The guide now walks through porting common React state patterns to `atom.io`, including `useState`, context state, Zustand stores, React Query, `useEffect` synchronization, timers, and transaction-based event handlers.
