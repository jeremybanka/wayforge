---
"atom.io": patch
---

🏷️ `atom.io/realtime-server` Improved the type signature for `realtimeStateReceiver()` to allow for the admission of more narrowly typed client states than server states.
