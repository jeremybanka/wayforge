---
"atom.io": patch
---

🎁 `atom.io/transceivers/o-list` brings the new `OList` (ordered list) transceiver. `OList` is a fully-featured `Array` that emits any mutations made to it as valid remote procedure calls to another `OList`.
