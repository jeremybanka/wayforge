---
"atom.io": patch
---

🐛 `atom.io/react` Fixed a bug with `useI()` where the target state would not be updated when this hook was used in "streamlined" (familyToken, key) mode and the passed key changed.
