---
"comline": minor
---

Re-export `ExpandCaptures` in place of the `Deref` type utility. Update imports of `Deref` from `comline` to `ExpandCaptures`. The utility accepts `CapturePrefix` and `RestMarker` parameters for expanding capture placeholders in string tuples.
