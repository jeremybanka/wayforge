---
"atom.io": patch
---

🎨 `atom.io/web` Gave clear names for the callbacks in `storageSync()` and `searchParamSync()`, making the stack traces for any errors thrown within these effects easier to read.
