---
"break-check": minor
---

Reserve `-v` for verbose output and use `-g` for the release tag pattern, eliminating the collision between the two options. Replace uses of `-v` for a tag pattern with `-g` or the existing `--tagPattern` option.
