---
"comline": patch
---

Prefer literal routes that accept the full positional path, with fallback to capture branches when a literal branch cannot accept the remaining arguments. Consider every matching capture branch; when multiple captures accept the same path, declaration order determines the selected route.
