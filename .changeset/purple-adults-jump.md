---
"atom.io": patch
---

✨ `getRelations` now accepts a `split` option, which returns two atom families instead of one. These families are actually the same family under the hood, but provide different type signatures from the a and b sides of the join.
