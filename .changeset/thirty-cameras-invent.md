---
"atom.io": patch
---

✨ `atom.io/json` adds the `Entries` type, the `fromEntries` function, and the `FromEntries` type. `Entries` are an array of `[key, value]` tuples. `fromEntries` converts an array of up to ten entries to an object with full type safety.
