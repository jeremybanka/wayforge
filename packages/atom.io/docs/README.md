# atom.io docs

- `source/` contains the authored MDX docs, concepts, and exhibit source files.
- `agent/` is generated from `source/` during `bun run build:agent-docs`.
- Start with `agent/README.md` when using an AI agent in an installed `atom.io` project.
